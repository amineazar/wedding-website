/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed — Vercel auto-detects Next.js and handles everything
  images: { unoptimized: true },
};

export default nextConfig;
